{
  "buildCommand": "bun run build",
  "outputDirectory": "dist/client",
  "functions": {
    "api/server.ts": {
      "runtime": "nodejs22.x"
    }
  },
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/api/server.ts"
    }
  ]
}
